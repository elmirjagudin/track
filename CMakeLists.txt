cmake_minimum_required(VERSION 3.4)
project( orbtst )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( orbtst main.cpp )
target_link_libraries( orbtst ${OpenCV_LIBS} )
